.conatiner.mt-4
  .col-lg-12.d-flex.justify-content-center
    .row
      = link_to 'Add Form', new_admin_form_path, class: 'btn btn-secondary mr-3 ml-5 mb-3'
      .col-lg-12
        table.table-bordered
          thead
            tr
              th[scope="col"]
                | Name
              th[scope="col"]
                | Description
              th[scope="col"]
                | Status
              th[scope="col"]
                | Published
              th[scope="col"]
                | Actions
          tbody
            - @forms.each.with_index(1) do |form, i|
              tr
                td = form.title
                td = form.description
                td = form.status
                td = Date.today.to_s.to_date
                td width="320px"
                  = link_to 'publish', admin_forms_set_published_path(id: form.id), class: 'btn btn-secondary  mr-1'
                  = link_to 'edit', edit_admin_form_path(form.id),      class: 'btn btn-secondary mr-1 '
                  = link_to 'show', admin_form_path(form.id),      class: 'btn btn-secondary mr-1 '
                  = link_to 'delete', admin_form_path(form.id), method: :delete,                                            \
                  data: { confirm: 'Are you sure?' } ,  class: 'btn btn-danger mr-1 '
